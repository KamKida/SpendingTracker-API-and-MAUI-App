<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SpendingTrackerApp.Pages.SpendingPages"
             x:Class="SpendingTrackerApp.Pages.SpendingPages.EditSpendingPage"
             xmlns:converters="clr-namespace:SpendingTrackerApp.Converters"
             Title="Edytuj wydatek">

	<ContentPage.Resources>
		<ResourceDictionary>
			<converters:DecimalNullableConverter x:Key="DecimalNullableConverter"/>
		</ResourceDictionary>
	</ContentPage.Resources>

	<ScrollView>
		<Grid InputTransparent="{Binding BlockInteraction}">

			<Grid RowDefinitions="Auto, Auto, Auto"
                  ColumnDefinitions="*, *"
                  Padding="20"
                  RowSpacing="20">

				<!-- Główne informacje o wydatku -->
				<Border Grid.Row="0" Grid.ColumnSpan="2">
					<Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto, Auto"
                          ColumnDefinitions="Auto, *"
                          ColumnSpacing="20"
                          RowSpacing="20">

						<Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Obecnie:" FontSize="33"/>

						<ScrollView Margin="0,0,25,0"
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="2"
                                    Orientation="Horizontal"
                                    HorizontalOptions="End">
							<Label Text="{Binding Spending.Amount}"
                                   FontSize="33"
                                   HorizontalTextAlignment="End"/>
						</ScrollView>

						<Label Grid.Row="3" Grid.Column="0" Text="Zł." VerticalOptions="Center"
                               HorizontalOptions="Start" FontSize="35" TextColor="{StaticResource Positive}"/>

						<ScrollView Orientation="Horizontal"
                                    Grid.Row="3"
                                    Grid.Column="1">
							<Entry Text="{Binding SpendingRequest.Amount, Mode=TwoWay, Converter={StaticResource DecimalNullableConverter}}"
                                   Placeholder="00.00"
                                   HorizontalTextAlignment="End"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"
                                   Keyboard="Numeric"
                                   FontSize="35"
                                   TextColor="{Binding AmountEntryColor}"
                                   BackgroundColor="Transparent"/>
						</ScrollView>

						<Label Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Text="Kategoria:" FontSize="35"/>
						<ScrollView Orientation="Horizontal" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="End">
							<Label FontSize="35"
                                   Text="{Binding SpendingCategoryRequest.Name, TargetNullValue='Brak'}"/>
						</ScrollView>

						<Label Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Text="Powinno być:" FontSize="35"/>
						<ScrollView Orientation="Horizontal" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="End">
							<Label FontSize="35"
                                   Text="{Binding SpendingCategoryRequest.ShouldBe, TargetNullValue='Brak'}"/>
						</ScrollView>

						<Label Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Text="Różnica:" FontSize="35"/>
						<ScrollView Orientation="Horizontal" Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="End">
							<Label FontSize="35"
                                   Text="{Binding SpendingDifference}"
                                   TextColor="{Binding DifferenceColor}"/>
						</ScrollView>

						<Button Grid.Row="10" Grid.Column="0" Grid.ColumnSpan="2"
                                HorizontalOptions="Start" WidthRequest="150"
                                VerticalOptions="Center" Margin="0,20,0,0"
                                Text="Zmień"
                                Command="{Binding ShowCategoryListForSpendingCommand}"/>

						<Button Grid.Row="11" Grid.Column="0" Grid.ColumnSpan="2"
                                HorizontalOptions="Start" WidthRequest="150"
                                VerticalOptions="Center" Margin="0,20,0,0"
                                Text="Usuń"
                                BackgroundColor="{StaticResource Negative}"
                                Command="{Binding DeleteCategoryForSpendingCommand}"/>

						<Label Grid.Row="12" Grid.ColumnSpan="2" Text="Opis:" FontSize="35"/>
						<Border Grid.Row="13" Grid.ColumnSpan="2" Background="White">
							<Grid RowDefinitions="Auto, Auto" ColumnDefinitions="*, Auto, Auto">
								<Editor Grid.ColumnSpan="3"
                                        AutoSize="Disabled"
                                        HeightRequest="240"
                                        FontSize="20"
                                        VerticalOptions="Start"
                                        MaxLength="150"
                                        Text="{Binding Description}"/>
								<Label Grid.Row="1" Grid.Column="1"
                                       Text="{Binding DescriptionCount}"
                                       TextColor="{StaticResource Primary}"/>
								<Label Grid.Row="1" Grid.Column="2" Text="/150" TextColor="{StaticResource Primary}"/>
							</Grid>
						</Border>
					</Grid>
				</Border>

				<!-- Wiadomości błędów -->
				<Border Grid.Row="1" Grid.ColumnSpan="2"
                        Background="White"
                        Padding="10">
					<Label Text="{Binding Message}"
                           TextColor="{Binding MessageColor}"
                           LineBreakMode="WordWrap" />
				</Border>

				<!-- Akcje: Zapisz / Anuluj -->
				<Button Grid.Row="2" Grid.Column="0" WidthRequest="150"
                        VerticalOptions="Center" Margin="0,20,0,0"
                        Text="Zapisz" BackgroundColor="{StaticResource Positive}"
                        Command="{Binding EditSpendingCommand}"/>

				<Button Grid.Row="2" Grid.Column="1" WidthRequest="150"
                        VerticalOptions="Center" Margin="0,20,0,0"
                        Text="Anuluj" BackgroundColor="{StaticResource Negative}"
                        Command="{Binding CancelEditCommand}"/>

			</Grid>

			<!-- Kolekcja kategorii -->
			<Border Grid.Row="0" IsVisible="{Binding ShowCategories}">
				<ScrollView>
					<Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto, Auto, Auto">
						<CollectionView Grid.Row="2" ItemsSource="{Binding SpendingCategories}">
							<CollectionView.ItemTemplate>
								<DataTemplate>
									<Border Margin="0,10,0,0">
										<Border.GestureRecognizers>
											<TapGestureRecognizer
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type local:EditSpendingPage}}, Path=BindingContext.SelectCategoryForSpendingCommand}"
                                                CommandParameter="{Binding .}" />
										</Border.GestureRecognizers>
										<Grid RowDefinitions="Auto, Auto, Auto, *"
                                              ColumnDefinitions="Auto, *"
                                              ColumnSpacing="30">
											<Label Grid.Row="0" Grid.Column="0"
                                                   Text="{Binding CreationDate, StringFormat='{0:dd.MM.yyyy HH:mm}'}"/>
											<Label Grid.Row="1" Grid.Column="0" Text="{Binding Name}"/>
											<Label Grid.Row="2" Grid.Column="0" Text="Powinno być: "/>
											<ScrollView Orientation="Horizontal" Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2" HorizontalOptions="End">
												<Label Grid.Row="3" Grid.Column="0" Text="{Binding ShouldBe}" TextColor="{StaticResource Positive}"/>
											</ScrollView>
										</Grid>
									</Border>
								</DataTemplate>
							</CollectionView.ItemTemplate>
						</CollectionView>
					</Grid>
				</ScrollView>
			</Border>

			<!-- Ikona ładowania -->
			<Grid IsVisible="{Binding ShowLoadingIcon}"
                  BackgroundColor="#80000000"
                  ZIndex="999"
                  Padding="50">
				<ActivityIndicator IsRunning="{Binding RunLoadingIcon}"
                                   Color="White"
                                   WidthRequest="80"
                                   HeightRequest="80"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
			</Grid>

		</Grid>
	</ScrollView>
</ContentPage>
